diff --git a/qanatone/admin.html b/qanatone/admin.html
index c5b204c..3f1eb5e 100644
--- a/qanatone/admin.html
+++ b/qanatone/admin.html
@@ -559,9 +559,10 @@ bulten:{t:'Bülten',n:()=>(get('posts')||[]).length,f:()=>`
 
 soz:{t:'Müşteri sözleri',n:()=>(get('testimonials')||[]).length,f:()=>`
   <h2>Müşteri sözleri</h2><p class="sub">Kanal kartlarının altındaki <b>ince kırmızı bantta</b> sırayla döner. Hiç kayıt yoksa bant kendini gizler. Geçiş süresi <b>Görünüm & Hareket</b> sekmesinde.</p>
+  <div class="note"><b>Bant şu an KAPALI.</b> Sitedeki üç söz uydurmaydı; gerçek müşteri cümlesi girilmeden yayında görünmesin diye kapatıldı. Sözleri gerçekleriyle değiştirdikten sonra aşağıdaki anahtarla aç.</div>
+  <div class="sws">${sw('Bandı sitede göster','theme.testi.on')}</div>
   <div class="note calm"><b>En ikna edici biçim:</b> tek cümlelik somut bir an. "Çok memnun kaldık" değil,
   "ilk kez tanımadığım biri sitemizden bulup aradı" gibi.</div>
-  <div class="note"><b>Mevcut sözler uydurma.</b> Gerçek müşteri cümleleriyle değiştirilmeden yayına çıkmamalı.</div>
   ${list('testimonials','Söz',(it,i,p)=>`
     <div class="r2">${fld('İsim',p+'.name')}${fld('Ünvan (TR)',p+'.role.tr')}</div>
     ${fld('Ünvan (EN)',p+'.role.en')}
diff --git a/qanatone/build.js b/qanatone/build.js
index 1140fd5..44f3e4a 100644
--- a/qanatone/build.js
+++ b/qanatone/build.js
@@ -73,7 +73,7 @@ const ozet = v => crypto.createHash('sha1')
    netlify/functions'tan derleniyor. Yayın klasörüne kopyalansaydı
    fonksiyon kaynak kodu /netlify/functions/diagnose.js adresinden
    herkese okunur hâle gelirdi — gereksiz bilgi ifşası.                */
-const KOPYA = ['img', 'js', 'admin.html', '_headers', 'content.json'];
+const KOPYA = ['img', 'js', 'admin.html', '_headers', 'content.json', 'og.png'];
 const BEKLE = 260;               // her rotadan sonra render'ın oturması için
 
 /* ---------- yardımcılar ---------- */
diff --git a/qanatone/index.html b/qanatone/index.html
index 1a617fc..70daa0b 100644
--- a/qanatone/index.html
+++ b/qanatone/index.html
@@ -2089,6 +2089,10 @@ html.lowfx #globe .gov{backdrop-filter:none;-webkit-backdrop-filter:none;backgro
 /* ============ SÖZ BANDI (ince) ============
    Eski kırmızı bandın inceltilmiş hâli: yarı dolgu, küçük tip, küçük
    avatar. Veri yine C.testimonials; boşsa bant kendini gizler.         */
+/* 2026-08: panelden kapatılabilir. Varsayılan KAPALI — sitedeki üç söz
+   uydurmaydı. Gizleme display:none ile; bölüm DOM'da kalıyor, çünkü
+   kanonik bölüm dizisi (kural 74) main'in çocuklarını sayıyor.        */
+html.t-nosoz #sozband{display:none}
 .tband{background:var(--red);padding:clamp(28px,3.6vw,50px) 0;
   position:relative;overflow:hidden}
 .tband::after{content:'';position:absolute;inset:0;pointer-events:none;opacity:.14;
@@ -5289,7 +5293,12 @@ theme:{
  hero:{hw:33,hx:-2,hy:-2,rw:33,rx:-2,ry:-2,mask:10,
        ringW:2,ringSpd:3.6,ringTail:56,ringGlow:7},
  map:{speed:1,rays:22,packets:1},
- testi:{interval:7}
+ /* testi.on: müşteri sözleri bandı (#sozband) açık mı. VARSAYILAN KAPALI —
+    sitedeki üç söz uydurmaydı; gerçek müşteri cümlesi girilene kadar bant
+    görünmüyor. Veri ve panel sekmesi duruyor, silinmedi: Enes gerçek
+    sözleri yazınca panelden tek tıkla açılır. Bant kapalıyken tasarımda
+    boş kırmızı şerit kalmıyor.                                          */
+ testi:{interval:7,on:0}
 },
 settings:{whatsapp:'905326291508',waMessage:"Merhaba, QANATONE sitesinden yazıyorum.",email:'',demoWa:'',gtm:'',assistant:true},
 partners:['Mercedes-Benz','Charles Schwab','Bab İç Mimarlık'],
@@ -6233,10 +6242,33 @@ faq:[
 
 founder:{photo:'img/founder.webp',name:'Enes Başoğlu',
  role:{tr:'Kurucu — SEO, reklam ve yapay zekâ otomasyonu',en:'Founder — SEO, ads and AI automation'},
- bio:{tr:'<p>Bu bölüm panelden düzenlenecek. Kurucunun kim olduğu, hangi işlerden geçtiği ve bu stüdyoyu neden kurduğu burada anlatılacak.</p><p>Ajansların anonim olduğu bir sektörde ismiyle ve yüzüyle duran biri olmak, sitedeki en güçlü güven sinyalidir.</p>',
-      en:'<p>This section will be edited from the panel.</p>'},
- links:[{label:'LinkedIn',url:''},{label:'Instagram',url:''}]},
-socials:[],legal:{line:'',kvkk:''},
+ /* 2026-08: buradaki metin yer tutucuydu ("bu bölüm panelden düzenlenecek").
+    Yapılandırılmış veride Person olarak kodlanacağı için gerçek bilgiyle
+    değiştirildi — yer tutucu bir bio, şemaya yazıldığında yanlış beyan olur.
+    "4M$+ bütçe yönetimi" Enes'in doğruladığı ifadedir; devredilen
+    girişimlerin değerlemesi gizlilik gereği yazılmıyor.                  */
+ bio:{tr:'<p>Girişimciliğe kripto para sektöründe başladım; kriptografi ve yazılım o günden beri ilgi alanım. Kurduğum dört girişim gizlilik anlaşmasıyla devredildi, süreç boyunca <b>4M$+ bütçe</b> yönettim.</p>'+
+        '<p>Yaratıcı reklam süreçlerini teknolojiyi yakından izleyerek geliştirdim. 2023\'ten beri odağım yapay zekâ: Shopify, Amazon, Etsy, Gumroad ve TikTok Shop üzerinde kendi e-ticaret operasyonlarımı yürütüyorum ve bu operasyonlarda öğrendiğim her şeyi yapay zekâyla ölçekleme yöntemlerine dönüştürüyorum.</p>'+
+        '<p>QANATONE bu birikimin tek çatı altında toplanmış hâli: talebi yakalayan sistemleri kurar, ölçer ve işletmeye devrederiz. Ajansların anonim kaldığı bir sektörde ismiyle duran biri olmayı tercih ediyorum.</p>',
+      en:'<p>I started out in the crypto sector; cryptography and software have been my field ever since. Four ventures I founded were transferred under confidentiality agreements, and along the way I managed <b>$4M+ in budgets</b>.</p>'+
+        '<p>I built my creative advertising practice by tracking technology closely. Since 2023 my focus has been artificial intelligence: I run my own e-commerce operations across Shopify, Amazon, Etsy, Gumroad and TikTok Shop, and turn everything those operations teach me into methods for scaling with AI.</p>'+
+        '<p>QANATONE is that experience under one roof: we build the systems that capture demand, measure them, and hand them over to the business. In an industry where agencies stay anonymous, I would rather stand behind my own name.</p>'},
+ /* Kişisel LinkedIn — kurumsal sayfa henüz açılmadı. Şemada Person.sameAs
+    olarak kullanılacak; Instagram kurum hesabı olduğu için socials'ta.   */
+ links:[{label:'LinkedIn',url:'https://www.linkedin.com/in/enesbsgluu'}]},
+/* socials → şemada Organization.sameAs olarak basılıyor (schema(), soc) */
+socials:[{label:'Instagram',url:'https://www.instagram.com/qanatone'}],
+legal:{line:'© QANATONE',
+ kvkk:'<h3>Aydınlatma metni (KVKK)</h3>'+
+  '<p>Bu metin, 6698 sayılı Kişisel Verilerin Korunması Kanunu kapsamında, sitedeki formlar aracılığıyla paylaştığın bilgilerin nasıl işlendiğini açıklar.</p>'+
+  '<p><b>Veri sorumlusu:</b> Enes Başoğlu (gerçek kişi). İletişim: sitedeki e-posta ve WhatsApp bağlantıları.</p>'+
+  '<p><b>İşlenen veriler.</b> Demo talebi formunda ad soyad, e-posta adresi ve telefon numarası zorunlu; talebini anlatan serbest metin alanı isteğe bağlıdır. Bülten formunda yalnızca e-posta adresi alınır. Site kontrol aracına yazdığın alan adı bir kişisel veri değildir; yalnızca kontrolü çalıştırmak için kullanılır ve saklanmaz.</p>'+
+  '<p><b>Amaç.</b> Demo talebi verileri yalnızca o talebe dönüş yapmak için kullanılır; pazarlama listesine eklenmez. Bülten e-postası yalnızca yeni yazı bildirimi için kullanılır ve her bildirimde tek tıkla çıkabilirsin.</p>'+
+  '<p><b>Hukuki sebep.</b> Talebine cevap verilmesi için işleme, sözleşme öncesi görüşme kapsamındadır (KVKK m.5/2-c). Bülten kaydı açık rızaya dayanır ve rıza her zaman geri alınabilir.</p>'+
+  '<p><b>Yurt dışına aktarım.</b> Form kayıtları, sitenin barındırıldığı <b>Netlify</b> altyapısında (ABD) tutulur. Formu gönderdiğinde bu aktarımı da kabul etmiş olursun. Aktarım yalnızca talebinin iletilmesi ve saklanması amacıyla yapılır.</p>'+
+  '<p><b>Saklama.</b> Demo talepleri, görüşme kapanana kadar; en geç bir yıl içinde silinir. Bülten kaydı, sen çıkana kadar tutulur.</p>'+
+  '<p><b>Haklarınız.</b> KVKK m.11 uyarınca verine erişme, düzeltilmesini veya silinmesini isteme ve işlemeye itiraz etme hakkın var. Talebini sitedeki e-posta adresine iletmen yeterli; en geç otuz gün içinde yanıtlanır.</p>'+
+  '<p><b>Çerezler.</b> Site reklam veya takip çerezi kullanmaz. Yalnızca dil tercihi ve panel oturumu gibi işlevsel bilgiler tarayıcında tutulur.</p>'},
 chimg:['img/ch1.webp','img/ch2.webp','img/ch3.webp','img/ch4.webp','img/ch5.webp','img/ch6.webp'],
 pages:{}
 };
@@ -7094,6 +7126,10 @@ function applyTheme(t){
   cl.toggle('t-noagent',off(m.cursor));
   cl.toggle('t-mo-soft',m.level==='soft');
   cl.toggle('t-mo-off',m.level==='off');
+  /* Müşteri sözleri bandı — içerik anahtarı, hareket anahtarı değil.
+     Kapalıyken #sozband display:none; DOM'da duruyor, yalnız gizleniyor.
+     (Kaldırılsaydı kural 74'ün kanonik bölüm dizisi bozulurdu.)        */
+  cl.toggle('t-nosoz',off((t.testi||{}).on));
 
   /* sistem imlecini gizleme anahtarı — ajan modülü bu bayrağı okuyor */
   window.__noHideCursor=off(m.hideCursor);
@@ -11462,7 +11498,20 @@ function initHeroHands(gsap, ST, REDUCE){
         smoothWheel:true,wheelMultiplier:1,touchMultiplier:1.6});
       lenis.on('scroll',ST.update);
       gsap.ticker.add(t=>lenis.raf(t*1000));
-      gsap.ticker.lagSmoothing(0);
+      /* 2026-08 KASMA — ÖLÇÜMLE DOĞRULANMIŞ TEK KAZANÇ.
+         Burada `lagSmoothing(0)` vardı: Lenis'in belgelerindeki hazır
+         entegrasyon satırı. Yaptığı şey GSAP'ın gecikme korumasını
+         KAPATMAK — kare uzadığında GSAP kaybettiği zamanı telafi etmeye
+         çalışır, bu da bir sonraki kareyi uzatır ve kasma kendi kendini
+         besler. Canlıda ölçüldü (10 sn, ana sayfada kaydırarak):
+           lagSmoothing(0)      → geç kare %15, en uzun kare 125 ms
+           lagSmoothing(500,33) → geç kare  %9, en uzun kare  61 ms
+         Varsayılan değerler geri getirildi: kare 500 ms'yi aşarsa GSAP
+         telafi etmeyi bırakır ve o kareyi 33 ms saymış gibi ilerler.
+         Lenis kendi zamanını rAF'tan aldığı için senkron bozulmaz.
+         Bu blok yalnız masaüstünde koşuyor (innerWidth>880) — kasma
+         zaten orada bildirilmişti.                                    */
+      gsap.ticker.lagSmoothing(500,33);
       window.__lenis=lenis;
     }
 
diff --git a/qanatone/test/denetim.js b/qanatone/test/denetim.js
index 8fbf18a..f73e1b8 100644
--- a/qanatone/test/denetim.js
+++ b/qanatone/test/denetim.js
@@ -590,6 +590,53 @@ function ciktiDenetimi() {
        filtreYok && sayiTamam && miTemiz,
        `filtreYok=${filtreYok} sayi=${bildirimler.length} miTemiz=${miTemiz}`);
   }
+  /* 87 · GSAP gecikme koruması açık kalır (2026-08, ölçümle doğrulandı):
+     `gsap.ticker.lagSmoothing(0)` Lenis belgelerinden kopyalanmış hazır
+     satırdı ve GSAP'ın gecikme telafisini KAPATIYORDU: uzun bir kareden
+     sonra GSAP açığı kapatmaya çalışıp bir sonraki kareyi de uzatıyor,
+     kasma kendini besliyordu. Canlı ölçüm (10 sn, ana sayfa):
+       lagSmoothing(0)      → geç kare %15, en uzun kare 125 ms
+       lagSmoothing(500,33) → geç kare  %9, en uzun kare  61 ms
+     Biri `(0)`'a geri döndürürse kasma sessizce geri gelir — bu kural
+     tam olarak o dönüşü yakalar. */
+  {
+    const kaynakL = fs.readFileSync(SRC, 'utf8');
+    /* YORUMLAR AYIKLANIR: yukarıdaki açıklama ve index.html'deki yama
+       yorumu eski `lagSmoothing(0)` satırını birebir alıntılıyor; ham
+       aramada kural kendi belgesine takılıp yanlış kırmızı verir.
+       (Bugün üçüncü kez yaşandı — bkz. kural 82 ve 86.) */
+    const kod = kaynakL.replace(/\/\*[\s\S]*?\*\//g, '');
+    const kapaliDegil = !/lagSmoothing\(\s*0\s*\)/.test(kod);
+    const korumaVar = /gsap\.ticker\.lagSmoothing\(\s*500\s*,\s*33\s*\)/.test(kod);
+    ol('GSAP gecikme koruması açık (lagSmoothing kapatılmamış)',
+       kapaliDegil && korumaVar,
+       `kapaliDegil=${kapaliDegil} korumaVar=${korumaVar}`);
+  }
+  /* 88 · içerik dürüstlüğü kapısı (2026-08, mimari Faz 1 ön şartı):
+     Kurucu bio'su yapılandırılmış veride Person olarak kodlanacak;
+     yer tutucu bir metin şemaya yazıldığında yanlış beyan olur. Müşteri
+     sözleri de Review olarak kodlanabilir — uydurma söz + şema, Google'ın
+     yapılandırılmış veri politikasının doğrudan ihlali (cezası manuel
+     işlem). Bu yüzden ikisi de kaynak seviyesinde kilitleniyor:
+       · bio'da yer tutucu kalıbı geçmeyecek
+       · sözler uydurma olduğu sürece bant KAPALI kalacak (testi.on:0)
+       · kurucunun en az bir sameAs bağlantısı olacak (varlık eşleştirmesi)
+     Sözler gerçekleriyle değişince Enes panelden açar; o gün bu kuralın
+     `bantKapali` şartı düşer ve kural güncellenir — bilinçli kapı. */
+  {
+    const kaynakI = fs.readFileSync(SRC, 'utf8');
+    const kod = kaynakI.replace(/\/\*[\s\S]*?\*\//g, '');
+    const yerTutucuYok = kod.indexOf('Bu bölüm panelden düzenlenecek') === -1
+                      && kod.indexOf('This section will be edited from the panel') === -1;
+    const bantKapali = /testi:\{[^}]*on:0/.test(kod);
+    const iF = kod.indexOf('founder:{');
+    const fBlok = iF > -1 ? kod.slice(iF, iF + 3000) : '';
+    const kurucuBagi = /links:\[\{label:'[^']+',url:'https?:\/\/[^']+'/.test(fBlok);
+    const kurumBagi = /socials:\[\{label:'[^']+',url:'https?:\/\/[^']+'/.test(kod);
+    ol('içerik dürüstlüğü (yer tutucu yok · uydurma bant kapalı · sameAs var)',
+       yerTutucuYok && bantKapali && kurucuBagi && kurumBagi,
+       `yerTutucuYok=${yerTutucuYok} bantKapali=${bantKapali} kurucuBagi=${kurucuBagi} kurumBagi=${kurumBagi}`);
+  }
   const sayfalar = [];
   (function tara(d) {
     for (const f of fs.readdirSync(d)) {
