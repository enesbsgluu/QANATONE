diff --git a/qanatone/index.html b/qanatone/index.html
index 893a8e2..d018922 100644
--- a/qanatone/index.html
+++ b/qanatone/index.html
@@ -525,7 +525,15 @@ html.lowfx #hero .hfloat::before,html.lowfx #hero .hfloat::after{display:none}
 /* maske farkı desteklenmiyorsa halkayı hiç gösterme — yoksa düğmeyi doldurur */
 @supports not ((-webkit-mask-composite:xor) or (mask-composite:exclude)){
   .void .ring{display:none}}
-html.lowfx .void .ring::before{animation:none}
+/* 2026-08 HALKA LOWFX'TE SUSUYORDU: burada `animation:none` vardı ve
+   Enes'in 8 çekirdekli makinesinde bile lowfx açıldığı için (Chrome'un
+   deviceMemory'yi ≤2 bildirmesi yeterli) halka HİÇ dönmüyordu — canlıda
+   ölçüldü: lowfx:true, anim:none, sure:0s, reduce:false. Oysa dönen şey
+   bir sözde-elemanın transform'u; derleyici katmanında koşar, düzen ve
+   boyama üretmez. Pahalı olan kısım halkanın üstündeki drop-shadow
+   parıltısıydı — her karede yeniden boyama ister. Artık lowfx animasyonu
+   değil PARILTIYI kaldırıyor: efekt görünür kalıyor, maliyet düşüyor.  */
+html.lowfx .void .ring{filter:none}
 @media(prefers-reduced-motion:reduce){.void .ring::before{animation:none}}
 .hmeta{display:flex;align-items:center;justify-content:center;gap:16px;margin-top:52px;
   flex-wrap:wrap;opacity:0;animation:fadeup 1s var(--e) 1s forwards;
diff --git a/qanatone/test/denetim.js b/qanatone/test/denetim.js
index 28ae067..0554e8c 100644
--- a/qanatone/test/denetim.js
+++ b/qanatone/test/denetim.js
@@ -551,9 +551,18 @@ function ciktiDenetimi() {
     const oranTutarli = bolen !== '' && bolen === yuzde;
     const korumalar = kaynakT.indexOf('@supports not ((-webkit-mask-composite:xor)') > -1
       && /prefers-reduced-motion:reduce\)\{\.void \.ring::before\{animation:none/.test(kaynakT);
-    ol('tarama yayı: hero halkası aralıklı + markup bağlı + oran tutarlı',
-       markupBagli && aralikli && oranTutarli && korumalar,
-       `markup=${markupBagli} aralikli=${aralikli} oran=${oranTutarli}(${bolen}/${yuzde}) korumalar=${korumalar}`);
+    /* 2026-08: lowfx halkayı SUSTURMAMALI. Canlıda ölçüldü — Enes'in
+       8 çekirdekli makinesinde lowfx açıktı (Chrome deviceMemory'yi ≤2
+       bildiriyor) ve `html.lowfx .void .ring::before{animation:none}`
+       yüzünden halka hiç dönmedi. Dönen şey bir sözde-elemanın
+       transform'u: derleyici katmanında koşar. Pahalı olan drop-shadow
+       parıltısıdır — lowfx onu kaldırır, animasyonu değil. */
+    const lowfxSusturmuyor =
+      kaynakT.indexOf('html.lowfx .void .ring::before{animation:none}') === -1
+      && kaynakT.indexOf('html.lowfx .void .ring{filter:none}') > -1;
+    ol('tarama yayı: hero halkası aralıklı + markup bağlı + oran tutarlı + lowfx susturmuyor',
+       markupBagli && aralikli && oranTutarli && korumalar && lowfxSusturmuyor,
+       `markup=${markupBagli} aralikli=${aralikli} oran=${oranTutarli}(${bolen}/${yuzde}) korumalar=${korumalar} lowfx=${lowfxSusturmuyor}`);
   }
   const sayfalar = [];
   (function tara(d) {
