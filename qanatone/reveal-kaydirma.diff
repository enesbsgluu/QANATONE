diff --git a/qanatone/index.html b/qanatone/index.html
index 6cdb430..b182521 100644
--- a/qanatone/index.html
+++ b/qanatone/index.html
@@ -7560,6 +7560,30 @@ function deliverStage(d,L,tx){
     const r=b.getBoundingClientRect();
     if(r.top<innerHeight&&r.bottom>0)b.classList.add('in');});
   window.__tara=tara;
+  /* 2026-08 MOBİL DİYAGRAM BOŞLUĞU (kadro + klasör şeması):
+     .in yalnız IO'dan ya da kurulum anındaki TEK ATIŞLIK geometri
+     kontrolünden geliyordu — kaydırmaya bağlı yedek YOKTU. Kutu kurulum
+     anında ekranın çok altındaysa ve IO herhangi bir sebeple tetiklenmezse
+     (mobilde adres çubuğu açılıp kapanırken yaşanan viewport değişimi
+     bunu yapabiliyor) içerik KALICI olarak görünmez kalıyordu: başlık ve
+     kök çip duruyor, .kdrow/.tpcard satırları opacity:0'da ölüyordu.
+     Artık kullanıcı o bölgeye geldiğinde senkron kontrol .in'i basıyor.
+     Tasarım ve animasyon aynı — yalnız "hiç açılmama" ihtimali kapandı.
+     rAF kısıtı zorunlu: ölçme aracının maliyeti kare bütçesinin altında
+     kalmalı (harita turunun dersi). Kalan kutu yoksa hiç ölçüm yapılmaz. */
+  let taraBekliyor = false;
+  const taraKisitli = () => {
+    if (taraBekliyor) return;
+    taraBekliyor = true;
+    requestAnimationFrame(() => {
+      taraBekliyor = false;
+      if (document.querySelector('.tpbox:not(.in),.kdbox:not(.in)')) tara();
+    });
+  };
+  addEventListener('scroll', taraKisitli, { passive: true });
+  addEventListener('resize', taraKisitli, { passive: true });
+  if (window.visualViewport)
+    visualViewport.addEventListener('resize', taraKisitli, { passive: true });
   const body=document.getElementById('sdBody');
   if(body&&'MutationObserver' in window)
     new MutationObserver(tara).observe(body,{childList:true});
diff --git a/qanatone/test/denetim.js b/qanatone/test/denetim.js
index 1ce2df8..6f2094f 100644
--- a/qanatone/test/denetim.js
+++ b/qanatone/test/denetim.js
@@ -415,6 +415,22 @@ function ciktiDenetimi() {
        varsayilanAnlik && geriYukleme && ikiliSifirlama,
        `anlık=${varsayilanAnlik} manual=${geriYukleme} ikili=${ikiliSifirlama}`);
   }
+  /* 79 · reveal ailesi kaydırmaya bağlı (2026-08, mobil diyagram boşluğu):
+     .kdrow/.tpcard varsayılan opacity:0; görünürlük kutuya inen .in'e
+     bağlı. .in yalnız IO'dan ve kurulum anındaki tek atışlık geometri
+     kontrolünden geliyordu — IO tetiklenmezse içerik KALICI görünmez
+     kalıyordu. Kaydırma bağlaması olmadan bu sınıf geri döner. */
+  {
+    const kaynakR = fs.readFileSync(SRC, 'utf8');
+    const iT = kaynakR.indexOf('window.__tara=tara;');
+    const pencere = iT > -1 ? kaynakR.slice(iT, iT + 1400) : '';
+    const kaydirma = pencere.indexOf("addEventListener('scroll'") > -1;
+    const boyut = pencere.indexOf("addEventListener('resize'") > -1;
+    const kisit = pencere.indexOf('requestAnimationFrame') > -1;
+    ol('reveal ailesi kaydırmaya bağlı (tek atışlık değil)',
+       iT > -1 && kaydirma && boyut && kisit,
+       `scroll=${kaydirma} resize=${boyut} rAF=${kisit}`);
+  }
   const sayfalar = [];
   (function tara(d) {
     for (const f of fs.readdirSync(d)) {
