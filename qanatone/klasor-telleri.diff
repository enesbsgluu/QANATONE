diff --git a/qanatone/index.html b/qanatone/index.html
index a17e9bd..68c80d7 100644
--- a/qanatone/index.html
+++ b/qanatone/index.html
@@ -3175,9 +3175,15 @@ a.ktcard:hover{background:#131317}
 .tpwires{position:absolute;inset:0;width:100%;height:100%;
   pointer-events:none;z-index:1;overflow:visible}
 .tpwires path{fill:none;stroke:rgba(255,255,255,.22);stroke-width:1.1;
-  vector-effect:non-scaling-stroke;stroke-dasharray:200;stroke-dashoffset:200;
+  vector-effect:non-scaling-stroke;stroke-dasharray:900;stroke-dashoffset:900;
   transition:stroke-dashoffset 1.1s var(--e);transition-delay:calc(.25s + var(--i)*.09s)}
-.tpbox.in .tpwires path{stroke-dashoffset:0}
+/* 900 = JS ÖLÇÜMÜ ÇALIŞMAZSA yedek. Eskiden 200'dü ve non-scaling-stroke
+   yüzünden 200 EKRAN PİKSELİ anlamına geliyordu: uzun teller (en üst ve
+   en alttaki) yarıda kesiliyordu. Gerçek uzunluk telHizala içinde
+   getTotalLength ile ölçülüp satır içi yazılıyor; buradaki değer yalnız
+   ölçüm yapılamadığı durumda devreye giriyor. !important şart: satır içi
+   dashoffset'i yenmesi gerekiyor, yoksa teller hiç açılmaz.           */
+.tpbox.in .tpwires path{stroke-dashoffset:0 !important}
 
 .tpcards{position:relative;z-index:2;display:flex;flex-direction:column;
   gap:clamp(8px,1vw,13px)}
@@ -3242,7 +3248,7 @@ a.ktcard:hover{background:#131317}
 @media(prefers-reduced-motion:reduce){
   .tppx{opacity:0}
   .tpleft,.tpcard{filter:none;opacity:1;transform:none}
-  .tpwires path{stroke-dashoffset:0}
+  .tpwires path{stroke-dashoffset:0 !important}
 }
 
 /* ============ PAZAR ZEKÂSI SAHNESİ (mk*) ============
@@ -7551,6 +7557,68 @@ function deliverStage(d,L,tx){
   document.addEventListener('focusin',e=>{
     const b=e.target.closest&&e.target.closest('.tpchip'); if(b)goster(b);});
 
+  /* 2026-08 TELLER KARTLARLA BİRLEŞMİYORDU:
+     deliverStage teller için y=(100/n)*(i+.5) kullanıyor, yani kartların
+     EŞİT YÜKSEKLİKTE ve ARALIKSIZ olduğunu varsayıyordu ("düzen ölçmeye
+     gerek yok" notu). İkisi de yanlış: .tpcards flex sütun + gap taşıyor
+     ve kart yükseklikleri içerik uzunluğuna göre değişiyor (TR/EN metin
+     farkı bile kaydırıyor). Sonuç: telin ucu kartın ortasını ıskalıyor.
+     Artık ucu gerçek kart merkezine taşıyoruz. Ölçüm TEK ATIŞ: kurulum,
+     yeniden boyutlandırma ve #sdBody değişimi. Kaydırmada ölçüm YOK —
+     kart yükseklikleri kaydırmayla değişmiyor, rAF döngüsünde düzen
+     okuma tuzağına girmemek için bilerek böyle.                        */
+  const telHizala = box => {
+    const mid = box.querySelector('.tpmid');
+    const svg = box.querySelector('.tpwires');
+    if (!mid || !svg) return;
+    const kartlar = [].slice.call(box.querySelectorAll('.tpcard'));
+    const yollar = [].slice.call(svg.querySelectorAll('path'));
+    if (!kartlar.length || yollar.length !== kartlar.length) return;
+    const mr = mid.getBoundingClientRect();
+    if (mr.height < 1) return;               /* henüz düzen yok, dokunma */
+    kartlar.forEach((k, i) => {
+      const kr = k.getBoundingClientRect();
+      if (kr.height < 1) return;
+      const y = ((kr.top + kr.height / 2) - mr.top) / mr.height * 100;
+      yollar[i].setAttribute('d',
+        'M0,50 C46,50 54,' + y.toFixed(1) + ' 100,' + y.toFixed(1));
+      /* 2026-08 TELLER HAVADA KESİLİYORDU: CSS'te stroke-dasharray:200
+         sabiti vardı ve vector-effect:non-scaling-stroke yüzünden bu 200
+         EKRAN PİKSELİ demek. 200'den uzun teller (en üstteki ve en
+         alttaki) yalnız ilk 200 piksel boyunca çiziliyor, gerisi hiç
+         görünmüyordu. Merkez hizalaması dış telleri daha da uzattığı için
+         belirginleşti. Sabit varsayım yerine ölçüm: her telin kendi
+         uzunluğu. +4 pay, yuvarlama hatası ucu kırpmasın diye.        */
+      if (yollar[i].getTotalLength) {
+        try {
+          const uz = Math.ceil(yollar[i].getTotalLength()) + 4;
+          yollar[i].style.strokeDasharray = uz;
+          yollar[i].style.strokeDashoffset =
+            box.classList.contains('in') ? 0 : uz;
+        } catch (e) {}
+      }
+    });
+  };
+  const tumTeller = () => {
+    const kutular = document.querySelectorAll('.tpbox');
+    for (let i = 0; i < kutular.length; i++) telHizala(kutular[i]);
+  };
+  window.__telHizala = tumTeller;
+  let telBekliyor = false;
+  const telKisitli = () => {
+    if (telBekliyor) return;
+    telBekliyor = true;
+    requestAnimationFrame(() => { telBekliyor = false; tumTeller(); });
+  };
+  addEventListener('resize', telKisitli, { passive: true });
+  if (window.visualViewport)
+    visualViewport.addEventListener('resize', telKisitli, { passive: true });
+  const tpGovde = document.getElementById('sdBody');
+  if (tpGovde && 'MutationObserver' in window)
+    new MutationObserver(telKisitli).observe(tpGovde, { childList: true });
+  if (document.fonts && document.fonts.ready) document.fonts.ready.then(telKisitli);
+  telKisitli();
+
   /* Beliriş: detay sayfalarında .rv anında açıldığı için kendi gözlemcimiz.
      Şema kadraja girince bulanık pikseller netleşiyor. */
   if(!('IntersectionObserver' in window))return;
@@ -8501,7 +8569,7 @@ function renderAll(){
      elementine kur-damgası basar; ikinci çağrı ve dil değişimi no-op olur. */
   ['__akLiveKur','__akisScrollKur','__clockKur','__tubesKur','__kanalKur',
    '__flowKur','__sizintiKur','__hesapKur','__projectsArchiveKur',
-   '__subscribeKur','__diagKur','__demandMapKur','__tara'].forEach(a=>{ if(window[a])guvenli(window[a],a); });
+   '__subscribeKur','__diagKur','__demandMapKur','__tara','__telHizala'].forEach(a=>{ if(window[a])guvenli(window[a],a); });
   if(window.__stRefresh)setTimeout(window.__stRefresh,140);
 }
 
diff --git a/qanatone/test/denetim.js b/qanatone/test/denetim.js
index 5af641d..e5ee699 100644
--- a/qanatone/test/denetim.js
+++ b/qanatone/test/denetim.js
@@ -447,6 +447,40 @@ function ciktiDenetimi() {
        iV > -1 && ekranaBagli && ustSinir,
        `${satir || 'kural bulunamadı'} · vh=${ekranaBagli} ust=${ustSinir}`);
   }
+  /* 81 · klasör telleri ölçülmüş kart merkezine hizalanır (2026-08):
+     deliverStage teller için y=(100/n)*(i+.5) basıyor — kartların eşit
+     yükseklikte ve aralıksız olduğu varsayımı. .tpcards flex sütun + gap
+     taşıyor ve kart yükseklikleri içeriğe göre değişiyor, o yüzden telin
+     ucu kartın ortasını ıskalıyordu (ölçümde 5.8 puana kadar sapma).
+     Hizalama kodu ve kurulum turuna kaydı birlikte kilitleniyor. */
+  {
+    const kaynakT = fs.readFileSync(SRC, 'utf8');
+    const hizalaVar = kaynakT.indexOf('const telHizala') > -1;
+    const merkezOlcumu = kaynakT.indexOf('kr.top + kr.height / 2') > -1;
+    const kurulumda = kaynakT.indexOf("'__telHizala'") > -1;
+    ol('klasör telleri ölçülmüş kart merkezine hizalanır',
+       hizalaVar && merkezOlcumu && kurulumda,
+       `hizala=${hizalaVar} merkez=${merkezOlcumu} kurulumTuru=${kurulumda}`);
+  }
+  /* 82 · tel uzunluğu ölçülür, sabit varsayılmaz (2026-08):
+     .tpwires path'te vector-effect:non-scaling-stroke var, yani
+     stroke-dasharray EKRAN PİKSELİ. Sabit 200 vardı ve 200'den uzun
+     teller (en üst/en alttaki) yarıda kesiliyordu. Üç şart: JS gerçek
+     uzunluğu ölçüyor · CSS yedeği ≥600 · .in kuralı satır içi değeri
+     yenecek önceliğe sahip (yoksa teller HİÇ açılmaz — bu ters yönde
+     bir tuzak, önceliği düşüren biri sessiz kırılma yaratır). */
+  {
+    const kaynakD = fs.readFileSync(SRC, 'utf8');
+    const iTH = kaynakD.indexOf('const telHizala');
+    const bolge = iTH > -1 ? kaynakD.slice(iTH, iTH + 2200) : '';
+    const olcum = bolge.indexOf('getTotalLength()') > -1
+               && bolge.indexOf('strokeDasharray') > -1;
+    const yedek = ((kaynakD.match(/\.tpwires path\{[^}]*stroke-dasharray:(\d+)/) || [, '0'])[1] * 1) >= 600;
+    const oncelik = /\.tpbox\.in \.tpwires path\{stroke-dashoffset:0 !important\}/.test(kaynakD);
+    ol('klasör telleri: uzunluk ölçülür, sabit varsayılmaz',
+       olcum && yedek && oncelik,
+       `olcum=${olcum} yedek=${yedek} oncelik=${oncelik}`);
+  }
   const sayfalar = [];
   (function tara(d) {
     for (const f of fs.readdirSync(d)) {
