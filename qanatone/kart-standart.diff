diff --git a/qanatone/index.html b/qanatone/index.html
index d2c1bc5..459c334 100644
--- a/qanatone/index.html
+++ b/qanatone/index.html
@@ -10361,7 +10361,12 @@ function projectsArchiveKur(){
 
   /* etiketin ilk parçası kategori olur: "Otomotiv · Bayi ağı" → "Otomotiv" */
   const cat=p=>String((typeof T==='function'?T(p.tag):'')||'').split('·')[0].trim();
-  const VH=[214,272,170,240,304,188,254];   /* görsel bloğu yükseklikleri — ritim */
+  /* 2026-08 STANDART KART: eskiden VH=[214,272,170,240,304,188,254]
+     dizisi karta SIRAYA göre 7 farklı görsel yüksekliği veriyordu
+     ("ritim"). Enes'in kararı: kartların standart bir boyutu olmalı,
+     görsel karta ölçeklenmeli (object-fit:cover zaten yapıyor).
+     --vh artık hiç atanmıyor → CSS varsayılanı (220px, mobilde ×.82)
+     her kartta aynı.                                                 */
 
   function render(){
     const P=(typeof C!=='undefined'&&C.projects)||[];
@@ -10373,13 +10378,15 @@ function projectsArchiveKur(){
       a.href='/projeler/'+p.slug;
       a.setAttribute('data-pd',p.slug);
       a.dataset.cat=cat(p);
-      a.style.setProperty('--vh',VH[i%VH.length]+'px');
       const hv=hueVars(i);
       a.style.setProperty('--g1',hv.g1); a.style.setProperty('--g2',hv.g2);
+      /* ilk 3 kart ekran üstünde: lazy bekletmesi "kartlar sırayla
+         geliyor" hissinin parçasıydı — onlar eager, gerisi lazy.     */
+      const yukle=(i<3?'decoding="async"':'loading="lazy" decoding="async"');
       a.innerHTML=
         '<span class="mi-t">'+
         '<span class="mv" data-pj="'+p.slug+'">'+((p.imgc||p.image)
-          ? '<img src="'+(p.imgc||p.image)+'" alt="" loading="lazy">'
+          ? '<img src="'+(p.imgc||p.image)+'" alt="" '+yukle+'>'
           : '<span class="mk"></span>')+
           '<span class="yr"></span></span>'+
         '<span class="mb"><span class="mt"></span><h3></h3><p></p>'+
diff --git a/qanatone/test/denetim.js b/qanatone/test/denetim.js
index 9eacbe6..c421d12 100644
--- a/qanatone/test/denetim.js
+++ b/qanatone/test/denetim.js
@@ -501,6 +501,21 @@ function ciktiDenetimi() {
        mobildeAcik && resizeBagisik,
        `mobildeAcik=${mobildeAcik} resizeBagisik=${resizeBagisik}`);
   }
+  /* 84 · proje kartları standart boyut (2026-08, Enes'in kararı):
+     VH ritim dizisi karta sıraya göre 7 farklı görsel yüksekliği
+     veriyordu; kaldırıldı — --vh atanmaz, CSS varsayılanı (220px)
+     her kartta aynı. Ayrıca ilk kartlar eager: hepsi lazy olunca
+     kartlar görsel yüklendikçe "sırayla geliyor"du. */
+  {
+    const kaynakK = fs.readFileSync(SRC, 'utf8');
+    const ritimYok = kaynakK.indexOf('VH[i%VH.length]') === -1
+                  && !/setProperty\('--vh'/.test(kaynakK);
+    const varsayilanDuruyor = kaynakK.indexOf('height:var(--vh,220px)') > -1;
+    const eagerVar = /i<3\?'decoding/.test(kaynakK);
+    ol('proje kartları standart boyut (+ilk kartlar eager)',
+       ritimYok && varsayilanDuruyor && eagerVar,
+       `ritimYok=${ritimYok} varsayilan=${varsayilanDuruyor} eager=${eagerVar}`);
+  }
   const sayfalar = [];
   (function tara(d) {
     for (const f of fs.readdirSync(d)) {
