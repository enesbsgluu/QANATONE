diff --git a/qanatone/index.html b/qanatone/index.html
index 826d513..826d400 100644
--- a/qanatone/index.html
+++ b/qanatone/index.html
@@ -6650,19 +6650,20 @@ window.__akisRender=function(){
        </span></a>`;
 };
 /* Dokuz mini sahne aynı anda dönmesin: bölüm kadraja girene kadar hepsi duruyor. */
-(function akLive(){
-  const sec=document.getElementById('akis'); if(!sec)return;
+function akLiveKur(){
+  const sec=document.getElementById('akis'); if(!sec||sec.__kur)return;sec.__kur=1;/* kur-damgası JS özelliği: dataset SERİLEŞİR ve statik HTML'e data-kur olarak basılıp tarayıcıda kurulumun atlanmasına yol açıyordu (2026-08'de yaşandı); __kur serileşmez */
   if(REDUCE||!('IntersectionObserver' in window)){sec.classList.add('aklive');return;}
   const io=new IntersectionObserver(es=>{
     sec.classList.toggle('aklive',es.some(e=>e.isIntersecting));
   },{threshold:.08});
   io.observe(sec);
-})();
+}
+akLiveKur();window.__akLiveKur=akLiveKur;
 
 /* Akış kaydırıcı: yerel yatay kaydırma + fare ile sürükleme + ok düğmeleri.
    Sürükleme 6px'i geçtiyse karttaki tıklama yutulur (yanlış gezinme olmaz). */
-(function akisScroll(){
-  const tr=document.getElementById('akTrack'); if(!tr)return;
+function akisScrollKur(){
+  const tr=document.getElementById('akTrack'); if(!tr||tr.__kur)return;tr.__kur=1;/* kur-damgası JS özelliği: dataset SERİLEŞİR ve statik HTML'e data-kur olarak basılıp tarayıcıda kurulumun atlanmasına yol açıyordu (2026-08'de yaşandı); __kur serileşmez */
   const step=()=>{
     const c=tr.querySelector('.akcard');
     return (c?c.getBoundingClientRect().width:320)+20;
@@ -6684,7 +6685,8 @@ window.__akisRender=function(){
   tr.addEventListener('click',e=>{
     if(moved>6){e.preventDefault();e.stopPropagation();moved=0;}
   },true);
-})();
+}
+akisScrollKur();window.__akisScrollKur=akisScrollKur;
 
 /* --- Teşhis başlığı: dikey kırpma içinden harf harf yukarıdan süzülme --- */
 window.__dgTitle=function(){
@@ -8443,6 +8445,11 @@ function renderAll(){
   if(window.__tstRender)guvenli(window.__tstRender,'musteri sozleri');
   guvenli(baglantilariDilleUyumla,'baglanti dili');
   guvenli(observeRv,'reveal');
+  /* 2026-08: kabuktan gelen bölümlerin kurulum turu. Her ...Kur kendi
+     elementine kur-damgası basar; ikinci çağrı ve dil değişimi no-op olur. */
+  ['__akLiveKur','__akisScrollKur','__clockKur','__tubesKur','__kanalKur',
+   '__flowKur','__sizintiKur','__hesapKur','__projectsArchiveKur',
+   '__subscribeKur','__diagKur'].forEach(a=>{ if(window[a])guvenli(window[a],a); });
   if(window.__stRefresh)setTimeout(window.__stRefresh,140);
 }
 
@@ -8509,8 +8516,8 @@ function observeRv(){
 })();
 
 /* =================== SAAT =================== */
-(function clock(){
-  const el=$('#clock');if(!el)return;
+function clockKur(){
+  const el=$('#clock');if(!el||el.__kur)return;el.__kur=1;/* kur-damgası JS özelliği: dataset SERİLEŞİR ve statik HTML'e data-kur olarak basılıp tarayıcıda kurulumun atlanmasına yol açıyordu (2026-08'de yaşandı); __kur serileşmez */
   /* Yanında "İstanbul · Dubai" yazıyor, o yüzden ziyaretçinin cihaz saatini
      değil İstanbul saatini gösteriyoruz. Londra'daki adam kendi saatini
      görüp "bunlar neyi ölçüyor" demesin.                                   */
@@ -8523,7 +8530,8 @@ function observeRv(){
     el.textContent=t;
   };
   up();setInterval(up,30000);
-})();
+}
+clockKur();window.__clockKur=clockKur;
 
 /* =================== NAV =================== */
 (function nav(){
@@ -8545,8 +8553,8 @@ function observeRv(){
    Sitedeki en ağır dosya; hero arkasındaki dekoratif WebGL katmanı.
    Bu yüzden kendiliğinden inmiyor: yalnızca applyTheme() "tüpler" anahtarını
    açık bulduğunda yükleniyor. Kapalıysa tek bayt bile inmiyor.           */
-(function tubes(){
-  const cv=$('#tubes');if(!cv)return;
+function tubesKur(){
+  const cv=$('#tubes');if(!cv||cv.__kur)return;cv.__kur=1;/* kur-damgası JS özelliği: dataset SERİLEŞİR ve statik HTML'e data-kur olarak basılıp tarayıcıda kurulumun atlanmasına yol açıyordu (2026-08'de yaşandı); __kur serileşmez */
   if(REDUCE||innerWidth<900||matchMedia('(pointer:coarse)').matches)return;
   let app=null,loading=false;
   const rnd=n=>[...Array(n)].map(()=>'#'+Math.floor(Math.random()*16777215).toString(16).padStart(6,'0'));
@@ -8600,7 +8608,8 @@ function observeRv(){
     });
   }).catch(()=>{loading=false;});
   };
-})();
+}
+tubesKur();window.__tubesKur=tubesKur;
 
 
 
@@ -9643,14 +9652,15 @@ function chImg(force){
   });
 }
 window.__chImg=chImg;
-(function kanal(){
-  const sec=$('#sozler'); if(!sec)return;
+function kanalKur(){
+  const sec=$('#sozler'); if(!sec||sec.__kur)return;sec.__kur=1;/* kur-damgası JS özelliği: dataset SERİLEŞİR ve statik HTML'e data-kur olarak basılıp tarayıcıda kurulumun atlanmasına yol açıyordu (2026-08'de yaşandı); __kur serileşmez */
   if(REDUCE||!('IntersectionObserver' in window)){sec.classList.add('tin');chImg(1);return;}
   const io=new IntersectionObserver(es=>{
     if(es.some(e=>e.isIntersecting)){sec.classList.add('tin');chImg(1);io.disconnect();}
   },{threshold:.16});
   io.observe(sec);
-})();
+}
+kanalKur();window.__kanalKur=kanalKur;
 /* 2026-08 BULUNDU: eskiden `const band=$('#sozband');if(!band)return;`
    EN ÜSTTE idi — /otomasyon gibi başka bir rota doğrudan açıldığında bant
    DOM'da olmadığından IIFE parse zamanında hemen dönüyor, window.__tstRender
@@ -9713,8 +9723,8 @@ window.__chImg=chImg;
 })();
 
 /* =================== İŞ AKIŞI (sektöre göre, akan) =================== */
-(function flow(){
-  const box=$('#flow');if(!box)return;
+function flowKur(){
+  const box=$('#flow');if(!box||box.__kur)return;box.__kur=1;/* kur-damgası JS özelliği: dataset SERİLEŞİR ve statik HTML'e data-kur olarak basılıp tarayıcıda kurulumun atlanmasına yol açıyordu (2026-08'de yaşandı); __kur serileşmez */
   const inC=$('#flowIn'),outC=$('#flowOut'),stC=$('#flowSteps'),
         wires=$('#flowWires'),pkts=$('#flowPkts'),svg=$('#flowSvg'),brain=$('#flowBrain');
   /* sektöre göre giriş kanalı ve çıkış aksiyonu haritası */
@@ -9873,7 +9883,8 @@ window.__chImg=chImg;
   window.__flowReveal=reveal;
   window.__flowWire=wire;
   if(REDUCE)reveal(1);
-})();
+}
+flowKur();window.__flowKur=flowKur;
 
 /* ── SIZINTI HUNİSİ ───────────────────────────────────────────────────
    İki motor: kaydırma AŞAMAYI sürer (hangi sızıntı göründü, hangisi
@@ -9887,8 +9898,8 @@ window.__chImg=chImg;
    aşamaya çıkarıldı ve her sızıntının YANINA onu KAPATAN mekanizma yazıldı.
    Asıl anlatılan şu: sorun listesi değil, sorun-çözüm eşleşmesi.
    RAKAM YOK: oran (kalan küme büyür) ve yön (maliyet çubuğu kısalır).   */
-(function sizinti(){
-  const st=document.getElementById('szStage'); if(!st)return;
+function sizintiKur(){
+  const st=document.getElementById('szStage'); if(!st||st.__kur)return;st.__kur=1;/* kur-damgası JS özelliği: dataset SERİLEŞİR ve statik HTML'e data-kur olarak basılıp tarayıcıda kurulumun atlanmasına yol açıyordu (2026-08'de yaşandı); __kur serileşmez */
   const cv=document.getElementById('szCv'); if(!cv)return;
   const cx=cv.getContext('2d'); if(!cx)return;
   const leaks=[...st.querySelectorAll('.szleak')];
@@ -10027,15 +10038,16 @@ window.__chImg=chImg;
   document.addEventListener('visibilitychange',()=>document.hidden?dur():basla());
   window.__szDraw=v=>{P=CL(v);if(RDC||!RAF)render(2600);};
   if(RDC){P=1;render(2600);}
-})();
+}
+sizintiKur();window.__sizintiKur=sizintiKur;
 
 /* ── KENDİ RAKAMINLA HUNİ ─────────────────────────────────────────────
    Ziyaretçi kendi sayılarını girer, huni onun rakamıyla akar.
    ÖNEMLİ: çıkan sonuç bizim performans iddiamız DEĞİL — girdiği
    yüzdelerin sıralı aritmetiği. "Şu kadarını kurtarırız" demiyoruz;
    kapanabilir kısmın keşifte konuşulacağı yazılı.                     */
-(function hesap(){
-  const box=document.getElementById('hsSec'); if(!box)return;
+function hesapKur(){
+  const box=document.getElementById('hsSec'); if(!box||box.__kur)return;box.__kur=1;/* kur-damgası JS özelliği: dataset SERİLEŞİR ve statik HTML'e data-kur olarak basılıp tarayıcıda kurulumun atlanmasına yol açıyordu (2026-08'de yaşandı); __kur serileşmez */
   const R=[0,1,2,3,4].map(i=>document.getElementById('hsR'+i));
   if(R.some(x=>!x))return;
   /* iş değeri: 500₺–2M₺ aralığını doğrusal kaydırıcı taşıyamaz (esnaf ile
@@ -10076,7 +10088,8 @@ window.__chImg=chImg;
   hesapla();
   /* dil değişince sayı biçimi de değişsin */
   window.__hsRefresh=hesapla;
-})();
+}
+hesapKur();window.__hesapKur=hesapKur;
 
 /* =================== SÜREÇ HATTI =================== */
 /* 2026-08 BULUNDU: eskiden `const box=$('#chan');if(!box)return;` EN
@@ -10144,9 +10157,9 @@ window.__chImg=chImg;
    Giriş: alttan + bulanıktan nete, kademeli. Filtre değişince kartlar
    yeni yerlerine kayar (yeniden çizilmez) — konum animasyonu GSAP'te.
    GSAP yoksa ya da hareket azaltma açıksa yerleşim anında uygulanır.   */
-(function projectsArchive(){
+function projectsArchiveKur(){
   const host=document.querySelector('#msn');
-  if(!host)return;
+  if(!host||host.__kur)return;host.__kur=1;/* kur-damgası JS özelliği: dataset SERİLEŞİR ve statik HTML'e data-kur olarak basılıp tarayıcıda kurulumun atlanmasına yol açıyordu (2026-08'de yaşandı); __kur serileşmez */
   const chips=document.querySelector('#msnFilter'), cnt=document.querySelector('#msnCount');
   const RDC=(typeof REDUCE!=='undefined')&&REDUCE;
   const G=()=>window.gsap;
@@ -10334,7 +10347,8 @@ window.__chImg=chImg;
 
   window.__prjAllRender=render;
   render();
-})();
+}
+projectsArchiveKur();window.__projectsArchiveKur=projectsArchiveKur;
 
 /* =================== FOOTER BİT DAMGASI ===================
    QANATONE yazısı bir kez çevrimdışı canvas'a çiziliyor, ızgaraya
@@ -10528,8 +10542,8 @@ window.__chImg=chImg;
 /* =================== BÜLTEN ABONELİĞİ ===================
    Netlify Forms'a "bulletin" adıyla düşer. Ayrı bir servise bağlamıyoruz;
    liste büyüyünce panelden dışa aktarıp istediğin araca taşırsın.      */
-(function subscribe(){
-  const f=$('#subForm'); if(!f)return;
+function subscribeKur(){
+  const f=$('#subForm'); if(!f||f.__kur)return;f.__kur=1;/* kur-damgası JS özelliği: dataset SERİLEŞİR ve statik HTML'e data-kur olarak basılıp tarayıcıda kurulumun atlanmasına yol açıyordu (2026-08'de yaşandı); __kur serileşmez */
   const msg=$('#subMsg');
   const S={tr:{send:'Gönderiliyor…',bad:'Geçerli bir e-posta yazar mısın?',
                ok:'Kaydettik. Yeni yazı çıkınca haber vereceğiz.',
@@ -10554,7 +10568,8 @@ window.__chImg=chImg;
     msg.textContent=ok?t.ok:t.off;
     if(ok&&el){el.value='';el.disabled=true;}
   });
-})();
+}
+subscribeKur();window.__subscribeKur=subscribeKur;
 
 /* =================== SEO: şema, canonical, hreflang, ölçüm ===================
    Kendi teşhis aracımız "yapılandırılmış veri yok", "adres tekrarı riski",
@@ -10950,10 +10965,10 @@ window.__introFor=(key)=>{
 };
 
 /* =================== CANLI TEŞHİS =================== */
-(function diag(){
+function diagKur(){
   const inp=$('#dgUrl'),go=$('#dgGo'),scan=$('#dgScan'),res=$('#dgRes'),
         err=$('#dgErr'),grid=$('#dgGrid'),fix=$('#dgFix'),arc=$('#dgArc');
-  if(!inp)return;
+  if(!inp||inp.__kur)return;inp.__kur=1;/* kur-damgası JS özelliği: dataset SERİLEŞİR ve statik HTML'e data-kur olarak basılıp tarayıcıda kurulumun atlanmasına yol açıyordu (2026-08'de yaşandı); __kur serileşmez */
   const L={tr:{
     https:'Güvenli bağlantı',status:'Site ayakta',speed:'Açılış hızı',weight:'Sayfa ağırlığı',
     title:'Sekme başlığı',desc:'Arama açıklaması',h1:'Ana başlık',canonical:'Adres tekrarı koruması',
@@ -11133,7 +11148,8 @@ window.__introFor=(key)=>{
     scrollToY(Math.max(0,box.offsetTop-90));
     setTimeout(()=>{const n=$('#ldName');if(n)n.focus({preventScroll:true});},700);
   };
-})();
+}
+diagKur();window.__diagKur=diagKur;
 
 /* ---- hero elleri: kaydırma + fare paralaksı ----------------------------
    Giriş animasyonu ve salınım CSS'te; burada sadece paralaks var.
@@ -11198,6 +11214,14 @@ function initHeroHands(gsap, ST, REDUCE){
     if(typeof window.Lenis!=='function'||!window.gsap||!window.ScrollTrigger){
       if(++tries>66)return fallback();      /* 66 × 90ms ≈ 6 sn */
       return setTimeout(boot,90);}
+    /* 2026-08 BULUNDU: sahneler parse anında, kabuk bölümleri getirmeden
+       kuruluyordu — statik iç sayfadan gelen ziyaretçide kadro/harita/deste
+       sahneleri hiç doğmuyordu (__stRefresh var olanı tazeler, olmayanı
+       YARATAMAZ). Kaynak modda sss+prjall parse'ta hazır: bekleme sıfır tur.
+       Statikte kabuk gelmezse mevcut 6 sn'lik vazgeçme + fallback korunuyor. */
+    if(!(document.body.dataset.tam==='1'||(document.getElementById('sss')&&document.getElementById('prjall')))){
+      if(++tries>66)return fallback();
+      return setTimeout(boot,90);}
     if(window.__motionOn)return;
     window.__motionOn=true;
     const gsap=window.gsap,ST=window.ScrollTrigger;
@@ -11364,9 +11388,21 @@ async function kabugu_tamamla(){
        olduğu gibi kalıyor. */
     const gecici=document.createElement('div');
     gecici.innerHTML=kabuk;
+    /* 2026-08 BULUNDU: eksikler main'in SONUNA ekleniyordu; statik hizmet
+       sayfasında #lead sondadır, ana sayfa bölümleri formun ARKASINA düşüyor
+       ve içeriden ana sayfaya geçen ziyaretçi formu en üstte görüyordu.
+       Eksikler artık #lead'i taşıyan main-seviye bloğun ÖNÜNE, kabuk
+       sırası korunarak giriyor: detay önde, form her rotada en sonda. */
+    let dayanak=null;
+    const leadEl=document.getElementById('lead');
+    if(leadEl){
+      dayanak=leadEl;
+      while(dayanak.parentElement&&dayanak.parentElement!==ana)dayanak=dayanak.parentElement;
+      if(dayanak.parentElement!==ana)dayanak=null;
+    }
     [...gecici.children].forEach(el=>{
       if(el.id&&document.getElementById(el.id))return;   // zaten canlı, dokunma
-      ana.appendChild(el);
+      if(dayanak)ana.insertBefore(el,dayanak); else ana.appendChild(el);
     });
     document.body.dataset.tam='1';
   }catch(e){ /* kabuk gelmezse sayfa tek rotalık kalır, çökmez */ }
diff --git a/qanatone/test/denetim.js b/qanatone/test/denetim.js
index 6a7a8fb..a863b77 100644
--- a/qanatone/test/denetim.js
+++ b/qanatone/test/denetim.js
@@ -541,6 +541,42 @@ async function statikDogrudanYukleme() {
      tbNavBtn > 0 && chanNodes > 0, `tbNav düğme=${tbNavBtn} chanNodes düğüm=${chanNodes}`);
 
   dom.window.close();
+
+  /* 2026-08 BULUNDU (gece denetimi): eksik bölümler main'in SONUNA eklenince
+     statik hizmet sayfasından ana sayfaya geçen ziyaretçi #lead formunu en
+     ÜSTTE görüyordu; ayrıca flow/sizinti/hesap/tubes/projectsArchive gibi
+     kurucular parse'ta bölümlerini bulamayınca kaydolmadan dönüyor, kabuk
+     bölümü getirse de kimse onları çağıramıyordu (kadro görünmez, harita 0).
+     İki kural: sıra + kayıt. Sayfa: hizmet detayı — kabuğa en muhtaç rota. */
+  {
+    const seoYolu = path.join(DIST, 'hizmetler', 'seo', 'index.html');
+    if (!fs.existsSync(seoYolu)) {
+      ol('dist/hizmetler/seo doğrudan yüklemede bölüm sırası: detay → kabuk → #lead', false, 'dist/hizmetler/seo yok');
+      ol('dist/hizmetler/seo doğrudan yüklemede geç kurucular kayıtlı', false, 'dist/hizmetler/seo yok');
+    } else {
+      const seoHtml = fs.readFileSync(seoYolu, 'utf8');
+      const { dom: dom2 } = ortam(seoHtml, ORIGIN + '/hizmetler/seo', { fetch: fetchMock });
+      const w2 = dom2.window, d2 = w2.document;
+      const t1 = Date.now();
+      while (!w2.__contentReady && Date.now() - t1 < 4000) await dur(40);
+      await dur(300);
+
+      const hepsi = [...d2.querySelectorAll('main *')];
+      const yeri = id => hepsi.findIndex(e => e.id === id);
+      const iDet = yeri('hizmetdetay'), iHero = yeri('hero'), iLead = yeri('lead');
+      ol('dist/hizmetler/seo doğrudan yüklemede bölüm sırası: detay → kabuk → #lead',
+         iDet > -1 && iHero > -1 && iLead > -1 && iDet < iHero && iHero < iLead,
+         `detay@${iDet} hero@${iHero} lead@${iLead}`);
+
+      const gec = ['__flowBuild','__flowWire','__flowReveal','__szDraw',
+                   '__hsRefresh','__tubesInit','__prjAllRender'];
+      const eksik = gec.filter(a => typeof w2[a] !== 'function');
+      ol('dist/hizmetler/seo doğrudan yüklemede geç kurucular kayıtlı',
+         eksik.length === 0, eksik.length ? 'eksik: ' + eksik.join(',') : gec.length + ' kurucu tamam');
+
+      dom2.window.close();
+    }
+  }
 }
 
 /* =====================================================================
